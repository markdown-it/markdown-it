// Default typograph options

'use strict';


module.exports = {
  singleQuotes: '‘’',
  doubleQuotes: '“”', // «» - russian, „“ - deutch
  copyright:    true, // (c) (C) -> ©
  trademark:    true, // (tm) (TM) -> ™
  registered:   true, // (r) (R) -> ®
  plusminus:    true, // +- -> ±
  paragraph:    true, // (p) (P) -> §
  ellipsis:     true, // ... -> …
  dupes:        true, // ???????? -> ???, !!!!! -> !!!, `,,` -> `,`
  emDashes:     true  // -- -> —
};
